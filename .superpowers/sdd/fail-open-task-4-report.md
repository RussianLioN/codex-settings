# Отчёт по этапу 4: живая приёмка установленной fail-open-системы

Дата проверки: 2026-08-06.

## Итог

Установленная пользовательская команда `codex` прошла десять
последовательных интерактивных циклов в чистом и изменённом Git-репозитории,
после нового коммита и в каталоге без Git. Обычный запуск, `resume --last`,
прерывание ответа и выход после прерывания не вызвали ни одного блокирующего
перехватчика.

Статус живой приёмки: `PASS`.

Этот статус относится к проверенной версии исходников и установленной
активации, указанным ниже. Он не заменяет отдельное независимое ревью перед
слиянием ветки.

## Проверенная установка

- Настоящий Codex: `/opt/homebrew/bin/codex`, версия `codex-cli 0.146.1`.
- Обычная команда в интерактивной оболочке:
  `codex='CODEX_SMART_ENABLED=1 $HOME/.local/bin/codex-highfd'`.
- Устаревший обязательный режим `CODEX_SMART_REQUIRED=1` в псевдониме
  отсутствует.
- `reconcile_codex_entrypoint.py --doctor --json` после испытаний:
  `ENTRYPOINT_READY`, `READY`, `changed=false`.
- Текущая активация:
  `act2_7b238d162abb78d073b3c89bfbd58d6ababb7bc0ed3f21f29938ea2511da272a`.
- Отпечаток источника:
  `2b422b8dbe158c0c1ea755e2b62098aa7181e42efcfe0c637933932693726185`.
- Каталог активаций и прежние неизменяемые активации сохранены; указатель
  `marketplace-current` атомарно направлен на проверенную активацию.

## Идемпотентность установщика

Последовательность выполнялась из текущей ветки против настоящего
пользовательского профиля:

1. Предварительный просмотр точки входа сообщил о требуемом изменении.
2. Применение точки входа завершилось `ENTRYPOINT_APPLIED`.
3. Её диагностика завершилась `ENTRYPOINT_READY`.
4. Первое применение умной надстройки завершилось `upgraded` и выбрало
   указанную выше активацию.
5. Второе применение без изменений завершилось `unchanged`.
6. Диагностика после установки сообщила `READY`, `FULL_READY`,
   `coordinatorStatus=SELECTED`, пустой список `problems`.
7. Повторный дымовой запуск после десяти циклов сообщил:

```json
{
  "launcherVersion": "codex-cli 0.146.1",
  "ok": true,
  "status": "FULL_READY"
}
```

## Десять последовательных циклов

Каждый процесс запускался через настоящий интерактивный псевдоним `codex` в
терминале с PTY уже после глобальной установки. Процессы выполнялись строго
последовательно и полностью закрывались перед следующим циклом.

| № | Контекст | Действие и наблюдение | Выход | Сеанс |
|---:|---|---|---:|---|
| 1 | чистый `repo-a` | Новый сеанс с коротким запросом; вызван `smart_plan`, получен ответ `готово`, выполнен `/exit` | 0 | `019fd7f5-d1d9-78a1-a9cc-b46dc3515ac3` |
| 2 | чистый `repo-a` | `resume --last` восстановил тот же сеанс; умное планирование началось, ответ намеренно прерван, затем `/exit` | 0 | `019fd7f5-d1d9-78a1-a9cc-b46dc3515ac3` |
| 3 | изменённый `repo-a` | Новый интерактивный сеанс без запроса дошёл до готового ввода и штатно закрылся | 0 | `019fd7f9-175e-72a0-8106-1cc8a5b86c67` |
| 4 | изменённый `repo-a` | `resume --last` штатно открыл последний содержательный сеанс каталога и закрылся без запроса | 0 | `019fd7f5-d1d9-78a1-a9cc-b46dc3515ac3` |
| 5 | `repo-b` после нового коммита | Новый интерактивный сеанс дошёл до готового ввода и штатно закрылся | 0 | `019fd7fa-31a3-7c40-9a55-535eab52e0d9` |
| 6 | тот же `repo-b` | `resume --last` открыл интерфейс без ошибки аренды и штатно закрылся | 0 | `019fd7fa-bb00-73a3-b9ab-cb49e8e1f66d` |
| 7 | тот же `repo-b` | Новый запрос; `UserPromptSubmit` завершился, умный режим активировался, ответ намеренно прерван, затем `/exit` | 0 | `019fd7fb-36a5-7711-a365-4ccb61dc1248` |
| 8 | тот же `repo-b` | `resume --last` восстановил сеанс № 7, принял новый запрос, дошёл до выполнения, был прерван и закрыт | 0 | `019fd7fb-36a5-7711-a365-4ccb61dc1248` |
| 9 | каталог без Git | Новый сеанс; после раннего контрольного прерывания запрос повторно отправлен в том же процессе, `UserPromptSubmit` завершился, умная маршрутизация началась, затем процесс штатно закрыт | 0 | `019fd7fd-6922-7b91-8cfc-ed08a1466b06` |
| 10 | тот же каталог без Git | `resume --last` восстановил сеанс № 9, принял новый запрос, начал обязательную умную маршрутизацию, был прерван и закрыт | 0 | `019fd7fd-6922-7b91-8cfc-ed08a1466b06` |

Циклы 4 и 6 также зафиксировали собственное поведение Codex для пустых
сеансов без пользовательского хода: `--last` выбирает последний доступный
возобновляемый сеанс, поэтому идентификатор не обязан совпадать с только что
открытым пустым интерфейсом. Это не подменялось логикой надстройки.

## Проверка запрещённых симптомов

Найдены шесть файлов журналов Codex, содержащих все десять циклов. По ним
выполнен точный поиск следующих признаков:

```text
MANAGED_RESUME_UNAVAILABLE
COORDINATOR_ACCOUNT_CATALOG_UNAVAILABLE
SOURCE_CHANGED
SessionStart hook (stopped)
UserPromptSubmit hook (stopped)
используйте codex-native
```

Результат поиска: 0 совпадений.

Во всех десяти циклах также отсутствовал код выхода 69. Все процессы
завершились с кодом 0, включая сценарии после намеренного прерывания.

## Процессы и ресурсы

- После закрытия десятого цикла поиск процессов по проверочным каталогам дал
  0 процессов.
- До живых циклов ограничитель показывал 81 глобальный процесс `node_repl`;
  после циклов он показывает те же 81. Следовательно, последовательная
  приёмка не добавила долговечную нить `node_repl`.
- Глобальный предохранитель по-прежнему сообщает `BLOCK`, потому что на
  компьютере открыты другие пользовательские сеансы и значение 81 превышает
  общий предел 80. Это не скрыто статусом `PASS`: новые субагенты до
  естественного освобождения ресурса запускаться не должны.
- Проверочная система не останавливала и не удаляла чужие сеансы.

## Проверки исходников перед установкой

Перед живой приёмкой полный проектный шлюз качества завершился успешно:

```text
make quality
Ran 2009 tests in 727.075s
OK (skipped=1)
код возврата 0
```

Проверки договоров, навигации, компиляции и `git diff --check`, входящие в
этот шлюз, также завершились успешно. В полном прогоне было одно
`ResourceWarning` о незакрытом соединении SQLite; оно не изменило результат
проверок. Отдельный осмотр изменённого пути диагностики подтвердил закрытие
его соединения в `finally`; возможная общая причина предупреждения не
выдаётся этим отчётом за устранённую без отдельного воспроизведения.

## Приёмочный вывод

Проверенная версия устраняет исходный пользовательский отказ: недоступность
доказательства умного пути больше не блокирует основной Codex, а доказанная
умная установка сохраняет запуск и возобновление через обычные команды
`codex` и `codex resume --last`. Живая приёмка завершена, но публикация и
слияние разрешены только после повторного независимого ревью исправлений
этапа 3 и финального шлюза качества.
